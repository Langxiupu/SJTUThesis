\chapter{基于任务反馈与协同增量重传的轻量化语义传输机制}
\label{chap:semantic}

\section{引言}

在端到端业务承载路径的可靠传输环节，星上分布式管控需要在星上算力、链路带宽和端到端时延受限条件下，提高任务相关信息的可靠恢复能力。LEO 卫星图像传输场景中，星上发送端保持轻量，仅负责图像获取与编码，地面接收端具备更强计算能力执行场景分类、目标识别等异构视觉任务。传统任务导向语义通信要求发送端预知任务模型或维护专用编解码器，这与星上载荷受限、任务在地面侧动态变化的运行方式不匹配。高性能语义传输中常用的大参数量 Transformer 编码或生成式复原模型会造成较高的星上计算、存储与推理时延开销。现有语义 HARQ 研究在重传触发、发送端任务先验需求和反馈粒度上仍有不足：重传触发多基于整体失真，难以定位与当前任务判决直接相关的语义缺口；发送端通常需要任务先验或较重的语义编码模型；反馈与跨轮合并机制多为粗粒度指示或固定融合。

面向上述受限链路下的任务相关语义可靠恢复问题，本章提出协同增量语义混合自动重传请求（Coordinated Incremental Semantic Hybrid Automatic Repeat Request，CIS-HARQ）机制，将重传从既有语义表征的重复发送重构为接收端反馈辅助的选择性语义增量重传。接收端依据当前任务输出定位任务相关语义缺口，并将其压缩为低开销硬掩码反馈；发送端利用该反馈生成面向请求分块的选择性语义增量；接收端通过跨轮语义累积保留有效历史语义并吸收当前增量。仿真结果表明，CIS-HARQ 以远小于高复杂度模型的参数规模，在窄带宽条件下通过多轮增量重传显著提升任务精度，星上推理时延保持在毫秒级，验证了以通信轮次缓解星上计算扩展的设计目标。本章相关成果已撰写成会议论文``Incremental Semantic HARQ for Heterogeneous Tasks on Resource-Constrained LEO Satellites''，投稿于IEEE International Conference on Computer Communications (INFOCOM) 2027。

\section{系统模型与问题建模}
\label{sec:ch5_sysmodel}

考虑由资源受限低轨卫星和地面接收端组成的非对称图像传输系统。星上发送端采集源图像并发送语义表征，地面接收端恢复图像表征后，调用本地任务模型完成下游推理。部署期间，星上发送端不获取任务标签与任务输出，也不存储或运行下游任务模型。训练期间，接收端计算的任务监督用于优化增量重传模块。每幅图像先经过一轮基础传输，随后可执行至多 $R$ 轮接收端驱动的增量语义重传。后续轮次只传送接收端请求的语义增量，以额外通信负载弥补轻量星上编码器单轮传输能力的不足。

本节主要符号汇总如表~\ref{tab:ch5_symbols} 所示。

\begin{longtable}[c]{@{}p{0.21\textwidth}p{0.245\textwidth}p{0.21\textwidth}p{0.245\textwidth}@{}}
\bicaption{CIS-R 系统模型主要符号汇总}{Main notation used in the system model of CIS-R}
\label{tab:ch5_symbols} \\
\toprule
符号 & 含义 & 符号 & 含义 \\
\midrule
\endfirsthead
\multicolumn{4}{l}{\textbf{续表~\thetable}} \\
\toprule
符号 & 含义 & 符号 & 含义 \\
\midrule
\endhead
\midrule
\multicolumn{4}{r}{续下页} \\
\endfoot
\bottomrule
\endlastfoot
  $\mathbf X,\ H,\ W,\ C_{\mathrm{img}}$ & 源图像、图像高、图像宽与图像通道数 & $f_{\mathrm{pat}},\ f_{\mathrm{unpat}}$ & 图像分块映射与反分块映射 \\
  $\mathbf P,\ \mathbf p_n,\ N,\ D$ & 分块特征序列、单分块特征、分块数与分块特征维度 & $\mathbf Y^{(r)},\ \mathbf y_n^{(r)}$ & 第 $r$ 轮接收端语义状态与第 $n$ 个分块状态 \\
  $\mathcal T_\phi,\ \hat{\mathbf X}^{(r)},\ \mathbf l^{(r)}$ & 冻结的接收端任务模型、恢复图像表征与任务输出向量 & $r,\ R,\ \mathbf e_r$ & 传输轮次、最大增量重传轮数与轮次嵌入 \\
  $f_{\mathrm{base}},\ \mathrm{Norm},\ g_{\mathrm{dec}}$ & 基础编码器、平均功率归一化与语义解码器 & $\mathbf Z^{(0)},\ \hat{\mathbf Z}^{(0)}$ & 基础传输负载与相应接收负载 \\
  $\mathbf M_{\mathrm{hard}}^{(r)},\ K,\ \mathcal I^{(r)}$ & 第 $r$ 轮二进制反馈掩码、选定分块数与分块索引集合 & $\mathcal F_{\mathrm{tx}},\ \tilde{\mathbf Z}^{(r)}$ & 反馈条件化增量编码映射与候选语义增量 \\
  $\operatorname{Pack},\ \mathbf Z^{(r)}$ & 所选分块打包操作与第 $r$ 轮实际发送负载 & $\hat{\mathbf Z}^{(r)},\ \mathbf N^{(r)},\ \sigma_r^2$ & 第 $r$ 轮接收负载、AWGN 噪声与噪声方差 \\
  $\gamma_r,\ P_r,\ D_r$ & 第 $r$ 轮信噪比、发送负载平均功率与实值传输维数 & $d(t_r),\ c,\ \tau_{\mathrm{prop}}^{(r)}$ & 星地斜距、光速与单向传播时延 \\
  $\epsilon,\ L^{(0)},\ L^{(r)}$ & 单分块实值信道维数、基础轮与增量轮信道使用次数 & $\mathrm{CBR}^{(r)},\ B_{\mathrm{fb}}^{(r)}$ & 累积信道带宽比与累积反馈比特数 \\
\end{longtable}

\noindent(1)~\textbf{非对称 LEO 图像传输场景}

记星上发送端采集的源图像为 $\mathbf X\in\mathbb R^{H\times W\times C_{\mathrm{img}}}$，其中 $H$、$W$ 与 $C_{\mathrm{img}}$ 分别表示图像高度、宽度和通道数。分块映射将源图像转换为
\begin{equation}\label{eq:ch5_patch}
\mathbf P=f_{\mathrm{pat}}(\mathbf X)=[\mathbf p_1,\dots,\mathbf p_N],\quad \mathbf p_n\in\mathbb R^{D},
\end{equation}
其中，$N$ 为图像分块数，$D$ 为单个分块的特征维度。星上发送端只观测源图像、当前传输轮次和接收端返回的二进制掩码，不访问任务标签、任务输出或任务模型参数。下游任务模型部署在算力充足的地面接收端，卫星无需为不同接收任务存储专用模型。

第 $r$ 轮传输结束后，地面接收端维护语义状态 $\mathbf Y^{(r)}=[\mathbf y_1^{(r)},\dots,\mathbf y_N^{(r)}]$，并通过反分块映射得到恢复图像表征 $\hat{\mathbf X}^{(r)}=f_{\mathrm{unpat}}(\mathbf Y^{(r)})$。冻结的接收端任务模型输出
\begin{equation}\label{eq:ch5_logits}
\mathbf l^{(r)}=\mathcal T_\phi\!\left(\hat{\mathbf X}^{(r)}\right),
\end{equation}
其中，$\mathbf l^{(r)}$ 为任务输出向量。接收端依据该向量评估当前语义状态，并生成下一轮分块反馈。任务监督和梯度计算均在地面侧完成，不占用星上计算资源。

\noindent(2)~\textbf{多轮语义增量重传模型}

协同增量语义重传（Coordinated Incremental Semantic Retransmission，CIS-R）包括一轮基础传输和至多 $R$ 轮接收端驱动的增量重传。记传输轮次为 $r\in\{0,1,\dots,R\}$，其中 $r=0$ 表示基础传输。星上发送端首先生成
\begin{equation}\label{eq:ch5_base}
\mathbf Z^{(0)}=\mathrm{Norm}\!\left(f_{\mathrm{base}}(\mathbf P)\right),
\end{equation}
其中，$f_{\mathrm{base}}(\cdot)$ 为基础编码器，$\mathrm{Norm}(\cdot)$ 执行平均功率归一化。基础负载经过前向信道后记为 $\hat{\mathbf Z}^{(0)}$，地面接收端将其解码为 $\mathbf Y^{(0)}=g_{\mathrm{dec}}(\hat{\mathbf Z}^{(0)})$。$\mathbf Y^{(0)}$ 不依赖具体下游任务，并作为后续增量重传的初始语义状态。

对任意增量重传轮 $r\geq1$，接收端生成二进制掩码 $\mathbf M_{\mathrm{hard}}^{(r)}\in\{0,1\}^{N}$，并满足 $\lVert\mathbf M_{\mathrm{hard}}^{(r)}\rVert_0=K$。该掩码指示本轮请求的 $K$ 个图像分块。星上发送端以源分块特征、反馈掩码和轮次嵌入为输入，生成候选语义增量
\begin{equation}
\tilde{\mathbf Z}^{(r)}=\mathcal F_{\mathrm{tx}}\!\left(\mathbf P,\mathbf M_{\mathrm{hard}}^{(r)},\mathbf e_r\right).
\end{equation}
定义所选分块的有序索引集合为 $\mathcal I^{(r)}=\{n:M_{\mathrm{hard},n}^{(r)}=1\}$。实际送入物理信道的负载仅包含这些分块：
\begin{equation}\label{eq:ch5_maskinc}
\begin{aligned}
\mathbf Z^{(r)}
&=\operatorname{Pack}\!\left(\tilde{\mathbf Z}^{(r)},\mathcal I^{(r)}\right)\\
&=\left[\tilde{\mathbf z}_{n}^{(r)}\right]_{n\in\mathcal I^{(r)}}
\in\mathbb R^{K\times\epsilon},\qquad r\geq1.
\end{aligned}
\end{equation}
接收端解码这 $K$ 行语义增量后，将其放回掩码指定的位置；未选位置在本轮增量中置零，再与历史语义状态累积。未被请求的分块不占用本轮前向链路资源。

\noindent(3)~\textbf{AWGN 前向链路与低开销反馈链路}

各轮语义负载通过加性白高斯噪声（Additive White Gaussian Noise，AWGN）前向链路传输。第 $r$ 轮接收负载为
\begin{equation}\label{eq:ch5_channel}
\hat{\mathbf Z}^{(r)}=\mathbf Z^{(r)}+\mathbf N^{(r)},
\end{equation}
其中
\begin{equation}
\mathbf N^{(r)}\sim\mathcal N\!\left(\mathbf 0,\sigma_r^2\mathbf I\right).
\end{equation}
给定第 $r$ 轮目标信噪比 $\gamma_r$（单位为 dB），噪声功率根据本轮实值发送张量的瞬时平均功率确定：
\begin{equation}
\sigma_r^2=\frac{P_r}{10^{\gamma_r/10}},\qquad
P_r=\frac{1}{D_r}\left\lVert\mathbf Z^{(r)}\right\rVert_2^2,
\end{equation}
其中，$D_r$ 为第 $r$ 轮发送负载包含的实值维数。静态仿真在一次训练和测试中采用固定 SNR。轨迹驱动仿真在每轮开始时从时变下行链路轨迹中读取 $\gamma_r$，并获取时刻 $t_r$ 的星地斜距 $d(t_r)$。相应的单向传播时延为
\begin{equation}
\tau_{\mathrm{prop}}^{(r)}=\frac{d(t_r)}{c},
\end{equation}
其中，$c$ 为光速。

反向链路仅传输二进制掩码 $\mathbf M_{\mathrm{hard}}^{(r)}$，并假定该掩码由可靠的低速率控制链路承载。每轮反馈固定占用 $N$ bit。对于 $14\times14$ 分块网格，单轮反馈量为 $196$ bit，三轮增量重传的累计反馈量为 $588$ bit。反馈中不携带高维语义特征、任务模型参数或任务输出，星上发送端只解析分块选择信息。

\noindent(4)~\textbf{通信开销与受限传输问题建模}

记每个发送分块占用 $\epsilon$ 个实值信道维数，并按两个实值维数对应一次复信道使用计数。基础轮与单个增量重传轮的前向负载分别为
\begin{equation}\label{eq:ch5_round_payloads}
L^{(0)}=\frac{N\epsilon}{2},\qquad
L^{(r)}=\frac{K\epsilon}{2},\quad r\geq1.
\end{equation}
截至第 $r$ 轮的累积信道带宽比（Channel Bandwidth Ratio，CBR）为
\begin{equation}\label{eq:ch5_cbr}
\mathrm{CBR}^{(r)}=\frac{\epsilon(N+rK)}{2HWC_{\mathrm{img}}},
\qquad r\in\{0,\ldots,R\}.
\end{equation}
反向链路累计开销单独记为
\begin{equation}
B_{\mathrm{fb}}^{(r)}=rN\quad\text{bit}.
\end{equation}
在上述模型中，$K$ 给出每轮请求的分块数量，$\epsilon$ 规定每个分块的前向负载，$R$ 限制增量重传次数。地面接收端依据当前任务输出选择待补充分块，星上发送端仅为这些分块生成并发送语义增量。本章研究的问题是：在星上发送端不部署下游任务模型且保持轻量编码的条件下，如何利用有限的前向增量负载和低速率反馈，提高地面接收端的任务性能。

\section{接收端驱动的协同增量语义重传框架设计}
\label{sec:ch5_framework}

协同增量语义重传（Coordinated Incremental Semantic Retransmission，CIS-R）用于星上发送端算力受限、地面接收端承担任务推理的非对称部署场景。卫星端执行图像分块、基础编码和反馈条件化语义增量生成，地面端负责语义解码、增量累积、下游任务推理和反馈生成。地面端根据当前任务输出选出需要补充语义信息的图像分块，卫星端据此生成并发送相应的语义增量。星上模型容量和推理开销保持不变，任务性能增益来自低速率反馈和有限的增量传输。

\begin{figure}[!htp]
    \centering
    \includegraphics[width=\linewidth]{figures/chapter6/figs/system_model.pdf}
    \bicaption{接收端驱动的 CIS-R 语义重传框架}{Receiver-driven semantic retransmission framework of CIS-R}
    \label{fig:ch5_system_model}
\end{figure}

\subsection{框架总览}
\label{subsec:ch5_protocol}

图~\ref{fig:ch5_system_model} 给出了 CIS-R 的收发结构。星上发送端由分块映射 $f_{\mathrm{pat}}$、基础编码器 $f_{\mathrm{base}}$ 和轻量反馈条件化增量生成模块 $\mathcal F_{\mathrm{tx}}$ 组成。地面接收端部署语义解码器 $g_{\mathrm{dec}}$、增量累积模块 $\mathcal F_{\mathrm{rx}}$、冻结的任务模型 $\mathcal T_\phi$ 以及反馈生成模块 $\mathcal F_{\mathrm{mask}}$。每轮接收完成后，地面端计算各分块对当前任务判决的敏感度，并将评分最高的 $K$ 个分块写入二进制掩码。该掩码通过反向控制链路回传，用于确定下一轮发送的分块及接收状态的更新位置。

算法~\ref{algo:ch5_cis} 给出了 CIS-R 的多轮收发过程。基础轮先建立任务无关的语义状态。此后，每个增量重传轮均由地面端发起分块请求，卫星端生成并打包对应的语义增量，地面端解码后将其并入已有语义状态。在线传输期间，卫星端无需访问任务标签、任务输出和任务模型参数。

\begin{algorithm}[!htp]
\caption{接收端驱动的协同增量语义重传（CIS-R）收发流程}
\label{algo:ch5_cis}
\KwIn{源图像 $\mathbf X$，最大增量重传轮数 $R$，每轮选择分块数 $K$，重复选择抑制因子 $\eta$，冻结的任务模型 $\mathcal T_\phi$}
\KwOut{完成第 $R$ 轮增量重传后的任务输出}
发送端计算分块特征 $\mathbf P=f_{\mathrm{pat}}(\mathbf X)$\;
发送端生成基础负载 $\mathbf Z^{(0)}=\mathrm{Norm}(f_{\mathrm{base}}(\mathbf P))$，并按式~\eqref{eq:ch5_channel} 经前向链路发送\;
接收端解码基础状态 $\mathbf Y^{(0)}=g_{\mathrm{dec}}(\hat{\mathbf Z}^{(0)})$\;
\For{$r\leftarrow 1$ \KwTo $R$}{
  接收端计算 $\mathbf l^{(r-1)}=\mathcal T_\phi(f_{\mathrm{unpat}}(\mathbf Y^{(r-1)}))$\;
  接收端按照式~\eqref{eq:ch5_margin}--\eqref{eq:ch5_penalty} 计算判决裕量和分块优先级\;
  接收端选取 Top-$K$ 分块生成 $\mathbf M_{\mathrm{hard}}^{(r)}$，并经反向链路回传\;
  发送端生成候选语义增量 $\tilde{\mathbf Z}^{(r)}=\mathcal F_{\mathrm{tx}}(\mathbf P,\mathbf M_{\mathrm{hard}}^{(r)},\mathbf e_r)$\;
  发送端按式~\eqref{eq:ch5_maskinc} 打包所选分块，并按式~\eqref{eq:ch5_channel} 发送 $\mathbf Z^{(r)}$\;
  接收端解码并按掩码位置恢复增量特征 $\mathbf U^{(r)}$\;
  接收端按式~\eqref{eq:ch5_accum} 更新语义状态 $\mathbf Y^{(r)}$\;
}
接收端输出 $\mathcal T_\phi(f_{\mathrm{unpat}}(\mathbf Y^{(R)}))$\;
\end{algorithm}

\subsection{任务驱动的决策敏感分块优先级评估}
\label{subsec:ch5_feedback}

第 $r$ 轮增量重传开始前，地面端根据当前任务判决对图像分块排序。由语义状态 $\mathbf Y^{(r-1)}$ 恢复图像表征 $\hat{\mathbf X}^{(r-1)}=f_{\mathrm{unpat}}(\mathbf Y^{(r-1)})$ 后，任务模型输出 $\mathbf l^{(r-1)}=\mathcal T_\phi(\hat{\mathbf X}^{(r-1)})$。对于分类任务，将最大任务输出与次大任务输出之差定义为判决裕量：
\begin{equation}\label{eq:ch5_margin}
O^{(r-1)}=l_{(1)}^{(r-1)}-l_{(2)}^{(r-1)}.
\end{equation}
其中，$l_{(1)}^{(r-1)}$ 和 $l_{(2)}^{(r-1)}$ 分别是 $\mathbf l^{(r-1)}$ 中的最大值和次大值。判决裕量对分块语义特征的梯度用于衡量当前任务输出对该分块的局部敏感度。

第 $n$ 个分块的优先级还与其当前特征幅度有关。为此，将梯度与特征激活相乘，得到分块归因评分
\begin{equation}\label{eq:ch5_attr}
C_n^{(r)}=\sum_{d=1}^{D}\left|\frac{\partial O^{(r-1)}}{\partial Y_{n,d}^{(r-1)}}\,Y_{n,d}^{(r-1)}\right|.
\end{equation}
上述梯度均在地面端计算，不占用星上计算资源。$C_n^{(r)}$ 越大，说明第 $n$ 个分块对当前任务判决的影响越大，增量重传时应优先选择该分块。

将归因评分进行最小--最大归一化，结果记为 $V_n^{(r)}$。若某个分块在相邻轮次连续入选，有限的增量传输资源可能过度集中于同一位置。因此，从第二轮增量重传开始，降低上一轮已选分块的评分：
\begin{equation}\label{eq:ch5_penalty}
\bar V_n^{(r)}=
\begin{cases}
\eta\,V_n^{(r)}, & M_{\mathrm{hard},n}^{(r-1)}=1,\\[2pt]
V_n^{(r)}, & M_{\mathrm{hard},n}^{(r-1)}=0,
\end{cases}
\quad r\ge2,
\end{equation}
其中，$\bar V_n^{(1)}=V_n^{(1)}$，$\eta\in[0,1]$ 为重复选择抑制因子。该处理仅下调近期已传分块的评分，仍允许对任务判决持续重要的分块再次传输。接收端从 $\bar{\mathbf V}^{(r)}$ 中选取评分最高的 $K$ 个分块，生成 $\mathbf M_{\mathrm{hard}}^{(r)}$。反向链路只需传输这一二进制掩码。

\subsection{反馈条件化语义增量生成}
\label{subsec:ch5_refine}

星上发送端接收到的任务反馈只有二进制掩码，不包含任务模型和任务输出。CIS-R 采用空间特征变换（Spatial Feature Transform，SFT），根据掩码和轮次信息生成分块级缩放参数与平移参数。第 $n$ 个分块的条件向量为 $\mathbf c_n^{(r)}=[M_{\mathrm{hard},n}^{(r)},\mathbf e_r]$，调制参数满足
\begin{equation}
[\boldsymbol\gamma_n^{(r)},\boldsymbol\beta_n^{(r)}]
=f_{\mathrm{SFT}}(\mathbf c_n^{(r)}).
\end{equation}
SFT 对原始分块特征的调制结果为
\begin{equation}\label{eq:ch5_sft}
\mathbf p_{n,\mathrm{mod}}^{(r)}=\boldsymbol\gamma_n^{(r)}\odot\mathbf p_n+\boldsymbol\beta_n^{(r)},
\end{equation}
其中，$\boldsymbol\gamma_n^{(r)}$ 与 $\boldsymbol\beta_n^{(r)}$ 分别为分块级缩放参数和平移参数。调制后的特征送入轻量残差增量网络：
\begin{equation}
\tilde{\mathbf z}_n^{(r)}=f_{\mathrm{res}}\!\left(\mathbf p_{n,\mathrm{mod}}^{(r)}\right).
\end{equation}
发送端按照式~\eqref{eq:ch5_maskinc} 将接收端请求的 $K$ 个分块打包为本轮负载。因而，每轮增量传输的前向链路占用只由 $K$ 和单分块实值信道维数 $\epsilon$ 决定，未选分块不产生额外的前向传输开销。

\subsection{掩码引导的增量语义累积}
\label{subsec:ch5_accum}

地面接收端对增量负载进行解码，并按反馈索引将所选分块回填至原位置，得到增量语义特征 $\mathbf U^{(r)}$。历史状态仍包含有效语义，当前增量各特征维的可靠性也不完全相同。CIS-R 根据二者联合生成融合门：
\begin{equation}\label{eq:ch5_accum}
\begin{aligned}
\mathbf G^{(r)}&=\sigma\!\left(f_g\!\left([\mathbf Y^{(r-1)},\mathbf U^{(r)}]\right)\right),\\
\tilde{\mathbf Y}^{(r)}&=\mathbf G^{(r)}\odot\mathbf Y^{(r-1)}+\left(1-\mathbf G^{(r)}\right)\odot\mathbf U^{(r)},\\
\mathbf Y^{(r)}&=\mathbf M_{\mathrm{hard}}^{(r)}\odot\tilde{\mathbf Y}^{(r)}+\left(1-\mathbf M_{\mathrm{hard}}^{(r)}\right)\odot\mathbf Y^{(r-1)},
\end{aligned}
\end{equation}
其中，$f_g(\cdot)$ 为融合门生成网络，$\sigma(\cdot)$ 为 sigmoid 函数。对于被请求的分块，$\mathbf G^{(r)}$ 逐维调节历史状态与当前增量的融合比例；其余分块不参与本轮更新，继续沿用 $\mathbf Y^{(r-1)}$ 中的状态。

\subsection{两阶段训练目标}
\label{subsec:ch5_train}

第一阶段训练任务无关的基础传输模块。分块映射、基础编码器、语义解码器和反分块映射采用图像重建损失联合训练：
\begin{equation}\label{eq:ch5_base_loss}
\mathcal L_{\mathrm{base}}
=\left\|f_{\mathrm{unpat}}\!\left(g_{\mathrm{dec}}(\hat{\mathbf Z}^{(0)})\right)-\mathbf X\right\|_2^2.
\end{equation}
这一阶段不使用下游任务模型，训练得到的基础语义状态作为地面端后续任务处理和增量累积的初始输入。

第二阶段冻结基础传输模块，只训练 SFT 参数生成网络、残差增量网络和接收端融合门。分类任务在第 $r$ 轮采用交叉熵损失 $\mathcal L_{\mathrm{task}}^{(r)}=\mathrm{CE}(\mathbf l^{(r)},y)$。为抑制过于平坦的预测分布，记
\begin{equation}
p_c^{(r)}=\mathrm{softmax}(\mathbf l^{(r)})_c,
\end{equation}
相应的预测熵为
\begin{equation}\label{eq:ch5_entropy}
\mathcal H^{(r)}=-\sum_c p_c^{(r)}\log\!\left(p_c^{(r)}+\delta\right),
\end{equation}
其中，$\delta>0$ 用于避免对数运算出现数值异常。第二阶段的训练目标写为
\begin{equation}\label{eq:ch5_loss}
\mathcal L_{\mathrm{CIS-R}}
=\sum_{r=1}^{R}\lambda_r\left(\mathcal L_{\mathrm{task}}^{(r)}+\lambda_{\mathrm{ent}}\mathcal H^{(r)}\right),
\end{equation}
其中，$\lambda_r$ 控制各增量重传轮对总损失的贡献，$\lambda_{\mathrm{ent}}$ 为预测熵正则项的权重。预测熵只约束地面端的任务输出，不用于估计传输码率。前向通信开销仍由式~\eqref{eq:ch5_round_payloads} 和式~\eqref{eq:ch5_cbr} 给出的分块数量与信道维数确定。

\section{仿真结果与分析}
\label{sec:ch5_sim}

本节从精度--CBR 折衷、计算复杂度与时延、模块消融、反馈可视化、敏感性与鲁棒性，以及不同数据集、多任务与跨任务泛化等方面，对 CIS-HARQ 进行系统评估。

\subsection{仿真设置}
\label{subsec:ch5_setup}

在三个公开数据集上评估 CIS-HARQ：ImageNet-100~\cite{deng-ImageNet-CVPR09} 与 CIFAR-100~\cite{krizhevsky-CIFAR-TR09} 用于单任务图像分类，VeRi-776~\cite{liu-VeRi776-ECCV16} 用于评估多任务接收端，其重建图像分别送入车辆颜色与车型两个任务头。除特别说明外，输入图像在传输前统一缩放或裁剪为 $224\times224$。前向链路建模为莱斯因子 $\kappa=10$ 的 Rician 衰落信道，以反映 LEO 链路 LoS 主导的几何特性；默认信噪比（Signal-to-Noise Ratio，SNR）为 $5$~dB，最大增量重传轮数为三轮，每个硬掩码反馈选取 $K=16$ 个分块。接收端任务模型采用视觉 Transformer（Vision Transformer，ViT）~\cite{dosovitskiy-ViT-ICLR21}。ImageNet-100 采用 ViT 特征维度与 JSCC 隐藏维度均为 $512$ 的配置，CIFAR-100 与 VeRi-776 采用维度分别为 $256$ 与 $128$ 的轻量配置，以检验受限配置下的反馈有效性。在 ImageNet-100 上，冻结任务模型对干净图像的精度为 $87.84\%$，作为相应图中的参照上界。主要仿真场景与模型超参数分别列于表~\ref{tab:ch5_scenario} 与表~\ref{tab:ch5_model}。

\begin{table}[!htp]
\centering
\bicaption{仿真场景与数据集配置}{Simulation scenario and dataset configuration}
\label{tab:ch5_scenario}
\small
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}l l@{}}
\toprule
参数 & 取值 \\
\midrule
数据集 & ImageNet-100；CIFAR-100；VeRi-776 \\
任务类型 & 图像分类；车辆颜色/车型分类（多任务） \\
输入尺寸 & $224\times224$ \\
前向信道 & Rician 衰落，$\kappa=10$ \\
默认 SNR & $5$~dB \\
最大重传轮数 $R$ & $3$ \\
反馈预算 $K$ & $16$ 个语义分块 \\
参照上界精度 & $87.84\%$（ImageNet-100，干净图像） \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[!htp]
\centering
\bicaption{模型结构与关键超参数}{Model structure and key hyperparameters}
\label{tab:ch5_model}
\small
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}l c c@{}}
\toprule
参数 & ImageNet-100 & CIFAR-100 / VeRi-776 \\
\midrule
任务模型骨干 & ViT & ViT \\
ViT 特征维度 $D$ & $512$ & $256$ \\
JSCC 隐藏维度 & $512$ & $128$ \\
单分块符号维度 $\epsilon$ & $\{16,32,64\}$ & 轻量固定配置 \\
反馈预算 $K$ & $16$ & $16$ \\
\bottomrule
\end{tabular}
\end{table}

对比基线为 SwinJSCC~\cite{SwinJSCC} 与 DiffJSCC~\cite{DiffJSCC}。SwinJSCC 作为对齐 CBR 设置下的静态开环 JSCC 基线；DiffJSCC 发布模型在本设置下仅提供 $\mathrm{CBR}=1/96$ 的单一工作点，且依赖 BLIP 与 Stable Diffusion、模型规模更大，故主要作为精度--复杂度参照而非完整 CBR 轨迹基线。评估指标包括分类精度、CBR、模型参数量与端到端时延。前向链路按复基带建模，统计 CBR 时两个实维计为一次复信道使用：对静态基线，$\mathrm{CBR}=NC_{\mathrm s}/(2HWC_{\mathrm{img}})$，其中 $C_{\mathrm s}$ 为静态 JSCC 基线的传输符号维度；对 CIS-HARQ 第 $r$ 轮则由式~\eqref{eq:ch5_cbr} 给出。CIS-HARQ 的附加传输开销由每轮请求分块数 $K$、单分块符号维度 $\epsilon$ 与重传轮数共同决定，并统一反映在 CBR 中。

\subsection{精度--CBR 主对比}
\label{subsec:ch5_acc_cbr}

表~\ref{tab:ch5_acc_cbr} 在 ImageNet-100、SNR $=5$~dB 下比较 SwinJSCC 与 CIS-HARQ 的分类精度和 CBR。低 CBR 工作点首先体现出闭环增量重传的收益：当 CBR 为 $0.0104$ 时，SwinJSCC 的精度为 $10.36\%$，而 CIS-HARQ 的基础轮 R0 已达到 $19.76\%$；经过三轮增量传输后，CBR 增至 $0.0129$，精度进一步升至 $38.40\%$，较 R0 提高 $18.64$ 个百分点。在中等 CBR 工作点，CIS-HARQ 由 $56.88\%$ 提升至 $65.02\%$，相对 R0 的增益为 $8.14$ 个百分点，同时仍明显高于同初始 CBR 下 SwinJSCC 的 $35.66\%$。这说明任务反馈引导的增量语义传输主要弥补初始语义状态不足，而不是简单依赖一次性增大发送符号维度。

当特征维度增至 $\epsilon=64$ 时，SwinJSCC 在 CBR $0.0417$ 下达到 $77.54\%$，CIS-HARQ 的 R0 精度为 $77.60\%$，二者几乎相同。继续执行三轮增量传输后，CIS-HARQ 的 CBR 从 $0.0417$ 增至 $0.0519$，最终精度仅提升到 $78.44\%$，相对 R0 增益为 $0.84$ 个百分点。该结果表明，当基础轮已经保留较充分的任务相关语义时，后续 HARQ 轮次带来的精度增益会自然收敛；CIS-HARQ 的主要适用区间是初始表征受链路带宽或信道质量限制的场景。

\begin{table}[!htp]
\centering
\bicaption{ImageNet-100 上信噪比 $5$~dB 时的分类精度与 CBR 对比}{Comparison of classification accuracy and CBR at SNR $5$~dB on ImageNet-100}
\label{tab:ch5_acc_cbr}
\small
\renewcommand{\arraystretch}{1.2}
\setlength{\tabcolsep}{5pt}
\begin{tabular}{@{}l c c c@{}}
\toprule
方法 & 配置 & CBR & 精度（\%） \\
\midrule
SwinJSCC & $C_{\mathrm s}=16$ & $0.0104$ & $10.36$ \\
CIS-HARQ & $\epsilon=16$，R0$\to$R3 & $0.0104\to0.0129$ & $19.76\to38.40$ \\
\midrule
SwinJSCC & $C_{\mathrm s}=32$ & $0.0208$ & $35.66$ \\
CIS-HARQ & $\epsilon=32$，R0$\to$R3 & $0.0208\to0.0259$ & $56.88\to65.02$ \\
\midrule
SwinJSCC & $C_{\mathrm s}=64$ & $0.0417$ & $77.54$ \\
CIS-HARQ & $\epsilon=64$，R0$\to$R3 & $0.0417\to0.0519$ & $77.60\to78.44$ \\
\bottomrule
\end{tabular}
\end{table}

图~\ref{fig:ch5_acc_cbr} 将 SwinJSCC 的静态开环工作点与 CIS-HARQ 的逐轮增量轨迹置于同一精度--CBR 平面。SwinJSCC 若要提高精度，只能切换到更大的静态符号维度；CIS-HARQ 则在 R0 基础上沿 HARQ 轮次追加少量前向链路开销。以 $\epsilon=16$ 为例，CIS-HARQ 在 CBR $0.0104$ 时的 R0 精度为 $19.76\%$，经过三轮增量传输后以 CBR $0.0129$ 达到 $38.40\%$；这一结果高于同 CBR 处 SwinJSCC 的 $10.36\%$，并超过 SwinJSCC 在更高 CBR $0.0208$ 下的 $35.66\%$。在 $\epsilon=32$ 时，R3 精度为 $65.02\%$，相对 R0 增加 $8.14$ 个百分点，但曲线斜率已低于 $\epsilon=16$；当 $\epsilon=64$ 时，R0 到 R3 仅增加 $0.84$ 个百分点，轨迹几乎水平。该趋势说明，反馈引导的增量传输主要在低、中 CBR 区间提升任务相关语义恢复效率；基础表征足够强时，继续增加重传轮次的收益有限。

\begin{figure}[!htp]
    \centering
    \includegraphics[width=0.8\linewidth]{figures/chapter5/accuracy_cbr_tradeoff.png}
    \bicaption{SNR为5 dB时CIS-HARQ的准确率与CBR权衡}{Accuracy-CBR tradeoff of CIS-HARQ at an SNR of 5 dB}
    \label{fig:ch5_acc_cbr}
\end{figure}

\subsection{计算复杂度与时延}
\label{subsec:ch5_complexity}

表~\ref{tab:ch5_complexity} 给出不同传输流程的模型规模与推理时延。此处时延为对应通信流程处理单幅图像的模型推理时间，不含传播时延与下游任务头推理；对 CIS-HARQ，时延涵盖 HARQ 流程中完整的收发推理管线。DiffJSCC 在其发布的 CBR 设置下达到 $85.60\%$ 精度，但依赖 BLIP 与 Stable Diffusion；SwinJSCC 较 DiffJSCC 更轻，但仍采用 Swin Transformer 骨干。CIS-HARQ 则由轻量基础编码器、基于多层感知机（Multilayer Perceptron，MLP）的增量重传模块与门控融合构成，参数量和推理时延分别为 $2.22$~M 与 $2.17$~ms。

\begin{table}[!htp]
\centering
\bicaption{不同传输流程的模型规模与推理时延}{Model size and inference latency of different transmission schemes}
\label{tab:ch5_complexity}
\small
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}l c c c@{}}
\toprule
方法 & 骨干 & 参数量（M） & 时延（ms） \\
\midrule
DiffJSCC & JSCC+BLIP+SD2.1 & $5479.00$ & $\sim4000.00$ \\
SwinJSCC & Swin Transformer & $33.03$ & $15.19$ \\
CIS-HARQ & 基础编码器+MLP+融合 & $2.22$ & $2.17$ \\
\bottomrule
\end{tabular}
\end{table}

综合表~\ref{tab:ch5_acc_cbr} 与表~\ref{tab:ch5_complexity} 可见，CIS-HARQ 在最强配置（$\epsilon=64$、R3）下达到 $78.44\%$ 精度，与 DiffJSCC 的 $85.60\%$ 相差 $7.16$ 个百分点，但其参数量仅为 $2.22$~M，约为 SwinJSCC 的 $1/15$、DiffJSCC 的 $1/2500$。从推理时延看，CIS-HARQ 为 $2.17$~ms，约为 SwinJSCC 的 $1/7$、DiffJSCC 的 $1/1800$。这表明，CIS-HARQ 把精度增益更多地放在窄带宽下的反馈增量轮次上，避免依赖更大的收发机容量。

 LEO 链路中的端到端时延由模型推理与往返传播共同决定。表~\ref{tab:ch5_complexity} 只计列模型推理时延，是为了隔离不同收发机结构的计算开销；传播时延主要由轨道高度、仰角和链路几何决定，而非由编解码器结构决定。对 CIS-HARQ 而言，每个增量重传轮会引入一次反馈闭环和一次额外前向传输，主要代价体现在 LEO 往返传播上；但其本地推理仍保持在毫秒级。相比之下，DiffJSCC 的 $\sim4000$~ms 推理时延来自生成式复原流程，即使不考虑传播也会占据主要时延预算。该分解说明，在星上算力受限而允许少量反馈轮次的场景下，用选择性增量重传替代模型容量扩展更符合 LEO 语义传输的部署约束。

\subsection{消融实验}
\label{subsec:ch5_ablation}

在 ImageNet-100、$\epsilon=32$、$K=16$、SNR $=5$~dB 下开展消融实验。图~\ref{fig:ch5_ablation} 将最终第三轮（R3）分类精度作为评价指标，分别考察语义状态累积和反馈分块选择两类设计。所有变体保持相同的增量轮数、单分块符号维度和反馈分块数，因此图中差异主要来自接收端如何融合跨轮语义状态，以及反馈模块如何选择后续请求分块。

\begin{figure}[!htp]
    \centering
    \includegraphics[width=0.7\linewidth]{figures/chapter5/round_wise_ablation.png}
    \bicaption{ImageNet-100下语义状态累积与反馈生成消融结果}{Ablation results of semantic state accumulation and feedback generation on ImageNet-100}
    \label{fig:ch5_ablation}
\end{figure}

语义累积消融显示，自适应融合门取得 $65.0\%$ 的 R3 精度，直接替换历史状态仅为 $57.2\%$，下降 $7.8$ 个百分点。这说明新一轮解码增量并不能无条件覆盖既有语义状态；在跨轮传输中，历史状态仍包含对最终分类有用的任务相关特征。简单平均达到 $64.7\%$，与自适应融合门仅差 $0.3$ 个百分点，表明在该工作点下，保留历史语义比复杂融合形式本身更关键。自适应融合门的主要作用是为不同分块提供可学习的历史--增量权重，增益幅度在该设置下较为有限。

反馈生成消融中，基于任务裕量的硬掩码达到 $65.0\%$，随机分块选择为 $61.0\%$，差距为 $4.0$ 个百分点。两者使用相同的 $K=16$ 和相同增量轮数，$4.0$ 个百分点的差距直接指向反馈分块选择策略。随机反馈仍能带来一定恢复能力，说明多轮增量本身可以补充部分丢失语义；基于裕量的反馈进一步把有限增量集中到影响分类边界的分块上，因而获得更高最终精度。

\subsection{反馈掩码可视化}
\label{subsec:ch5_maskvis}

 图~\ref{fig:ch5_maskvis} 可视化了 CIS-HARQ 的任务驱动硬掩码反馈。接收端在每一轮基于当前语义状态计算任务裕量归因，并选择 top-$K$ 分块作为下一轮增量请求；语义状态更新后，归因分布重新计算，因而 R1、R2 和 R3 的掩码位置并不固定。该反馈只回传分块索引，不传输高维语义特征，使反向链路开销保持在低控制信令量级。

\begin{figure}[!htp]
    \centering
    \includegraphics[width=\linewidth]{figures/chapter5/feedback_mask_vis_paper.png}
    \bicaption{CIS-HARQ任务驱动反馈掩码可视化}{Visualization of task-driven feedback masks in CIS-HARQ}
    \label{fig:ch5_maskvis}
\end{figure}

 从该样例可以看到，R1 的归因图主要在目标主体及其附近形成较高响应，后续硬掩码覆盖目标边缘、局部纹理以及与背景交界的区域。随着 R2 和 R3 更新语义状态，掩码位置发生调整，说明反馈并非重复请求固定区域，而是随当前判决状态重新选择增量分块。该样例在 R0 后分类错误、R3 后转为正确，提供了任务相关反馈能够引导互补增量重传的定性证据；定量收益仍以上一节消融结果中的 $4.0$ 个百分点反馈增益为准。

\subsection{特征维度敏感性}
\label{subsec:ch5_epsilon}

 进一步评估特征维度 $\epsilon$ 在 ImageNet-100、SNR $=5$~dB、$K=16$ 下的影响。图~\ref{fig:ch5_epsilon} 显示，当 $\epsilon=8$ 时，R0 精度仅约 $5\%$，经过三轮增量重传后也只有约 $10\%$。这说明首轮传输若只保留很少的任务相关语义，后续 HARQ 轮次可补充的信息有限。将 $\epsilon$ 增至 $16$ 后，CIS-HARQ 的精度由 R0 的 $19.76\%$ 提升至 R3 的 $38.40\%$，增益为 $18.64$ 个百分点；$\epsilon=32$ 时最终精度达到 $65.0\%$。该变化表明，中等特征维度能够提供更可靠的初始语义状态，使任务反馈有足够依据选择后续增量分块。

当 $\epsilon$ 增至 $64$ 和 $128$ 时，R0 精度已分别约为 $77.6\%$ 与 $83\%$，与低维配置相比，主要增益来自首轮语义承载能力的提升。此时继续执行增量重传的边际收益明显收窄：$\epsilon=64$ 从 R0 到 R3 仅提升到 $78.44\%$，增益为 $0.84$ 个百分点；$\epsilon=128$ 的逐轮曲线也基本保持平坦，并仍低于干净图像参照上界 $87.84\%$。因此，$\epsilon$ 主要决定首轮前向链路能够承载的语义信息量，而 HARQ 轮次更适合修补低、中 CBR 条件下首轮遗漏的任务相关分块；当初始状态已较完整时，继续增加重传轮次难以带来同等幅度的精度提升。

\begin{figure}[!htp]
    \centering
    \includegraphics[width=0.8\linewidth]{figures/chapter5/epsilon_sensitivity.png}
    \bicaption{ImageNet-100下特征维度对CIS-HARQ增量重传的影响}{Impact of feature dimension on CIS-HARQ incremental retransmission on ImageNet-100}
    \label{fig:ch5_epsilon}
\end{figure}

\subsection{信道信噪比鲁棒性}
\label{subsec:ch5_snr}

 图~\ref{fig:ch5_snr} 在 ImageNet-100 上比较 $\epsilon=32$ 与 $\epsilon=64$、$K=16$ 时 CIS-HARQ 在不同 SNR 下的 R0 与 R3 精度。对 $\epsilon=32$，SNR $=2$~dB 时 R0 精度约为 $30\%$，R3 提升至约 $48\%$，增益约 $18$ 个百分点；SNR $=5$~dB 时由约 $57\%$ 提升至约 $65\%$，增益约 $8$ 个百分点；SNR $=8$~dB 时仅由约 $70\%$ 提升至约 $71\%$。增益随 SNR 升高而收窄，说明信道噪声主要削弱首轮语义状态的完整性。在低 SNR 下，接收端任务反馈仍能从受损语义状态中定位后续请求分块，因此增量重传对分类精度的补偿更明显。

当特征维度增至 $\epsilon=64$ 时，各 SNR 工作点的 R0 精度已明显高于 $\epsilon=32$，R0 与 R3 曲线间距相应变小。图中 $2$~dB 下仍可观察到约数个百分点的重传收益，而在 $5$~dB 和 $8$~dB 下，R3 仅比 R0 略高，曲线接近重合。该现象与图~\ref{fig:ch5_epsilon} 的特征维度敏感性一致：较大的单分块符号维度提高了首轮前向链路的语义承载能力，HARQ 轮次主要用于修补剩余的任务相关缺口；当信道质量和首轮表征能力同时较好时，继续重传带来的精度增益自然有限。

\begin{figure}[!htp]
    \centering
    \includegraphics[width=0.8\linewidth]{figures/chapter5/snr_robustness.png}
    \bicaption{ImageNet-100下SNR对CIS-HARQ分类准确度的影响}{Impact of SNR on CIS-HARQ accuracy on ImageNet-100}
    \label{fig:ch5_snr}
\end{figure}

\subsection{不同数据集、多任务与跨任务泛化}
\label{subsec:ch5_generalization}

 图~\ref{fig:ch5_generalization}(a) 比较初始 R0 精度、随机反馈下的 R3 精度和任务驱动反馈下的 R3 精度。各数据集与任务设置均采用独立训练的 CIS-HARQ 模型，因此该实验不用于证明跨数据集迁移或零样本泛化，而用于检验反馈辅助增量重传在不同数据分布与任务接收端下是否持续有效。对单任务分类数据集，任务驱动反馈均优于随机反馈。CIFAR-100 上，随机反馈仅将精度由 $70.7\%$ 提升至 $70.8\%$，增益为 $0.1$ 个百分点；任务驱动反馈达到 $74.9\%$，较 R0 提高 $4.2$ 个百分点，较随机反馈高 $4.1$ 个百分点。ImageNet-100 上，随机反馈的 R3 精度为 $61.0\%$，任务驱动反馈达到 $65.0\%$，相对 R0 的增益分别为 $4.1$ 和 $8.1$ 个百分点。该对比说明，接收端任务输出提供的分块选择信息可在不同单任务数据集上稳定改善增量重传，而不是简单依赖随机追加前向链路开销。

 在 VeRi-776 多任务设置中，颜色与车型分类的 R0 精度分别为 $75.1\%$ 和 $35.4\%$。随机反馈已将两项任务的 R3 精度提升至 $88.1\%$ 和 $77.6\%$，说明当初始语义状态较弱时，多轮增量传输本身也能补充大量可判别信息。任务驱动反馈进一步达到 $89.0\%$ 和 $79.5\%$，较随机反馈分别增加 $0.9$ 和 $1.9$ 个百分点。该结果给出的结论更克制：CIS-HARQ 的闭环反馈在单任务与多任务接收端上均保持有效，但多任务场景中的主要增益同时来自增量传输轮次和任务相关分块选择，两者不能简单混为同一来源。


 为进一步区分接收端反馈有效性与发送端是否需要目标任务重训，本文补充冻结 ImageNet-100 发送端的 CIFAR-100 迁移实验。源发送端在 ImageNet-100 上训练，迁移到 CIFAR-100 后完全冻结；CIFAR-100 任务模型只放在接收端，用于生成硬掩码反馈。在线传输阶段，发送端不访问 CIFAR-100 分类器、logits、梯度或标签。这个设置不把 CIS-HARQ 宣称为完全任务无关系统，而是专门检验发送端在目标任务上不重训时，接收端能否仍通过任务驱动硬掩码完成增量重传。

\begin{figure}[!htp]
    \centering
    \begin{subfigure}[t]{0.49\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/chapter5/generalization_multitask.png}
        \caption{不同数据集与任务设置}
        \label{fig:ch5_generalization_datasets}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.49\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/chapter5/frozen_tx_transfer.png}
        \caption{冻结发送端跨任务迁移}
        \label{fig:ch5_frozen_tx_transfer}
    \end{subfigure}
    \bicaption{CIS-HARQ在不同数据集、多任务与跨任务迁移设置下的反馈有效性}{Feedback effectiveness of CIS-HARQ under different datasets, multiple tasks, and cross-task transfer}
    \label{fig:ch5_generalization}
\end{figure}


 图~\ref{fig:ch5_generalization}(b) 给出了两种接收端适配设置下的结果。在 \texttt{receiver\_all} 中，CIFAR-100 接收端的 decoder、unpatchify 与 fusion gate 均参与适配，精度由 R0 的 $73.15\%$ 提升到 R3 的 $77.27\%$。该结果说明冻结的 ImageNet-100 发送端能够提供可被 CIFAR-100 接收端解释的基础语义表征，但由于接收端适配自由度较大，它不适合作为最严格的迁移证据。

更严格的是 \texttt{fusion\_only} 设置。Stage 1 只训练 CIFAR-100 接收端的 decoder 与 unpatchify，以解释冻结发送端输出的基础表征；Stage 2 只训练 fusion gate，发送端始终不更新参数。在这一限制下，CIS-HARQ 仍由 R0 的 $70.64\%$ 提升到 R3 的 $75.20\%$，与 CIFAR-100 原生 scaled 配置的 R3 精度 $75.36\%$ 仅相差 $0.16$ 个百分点。两组迁移实验的增量比特也处于相同量级，\texttt{fusion\_only} 在 R1、R2 和 R3 的估计比特数分别为 $1007.05$、$991.23$ 与 $64.79$。因此，在发送端目标任务不重训设置下，性能接近原生训练并不是由额外前向负载换来的，而是来自接收端硬掩码对已有发送端表征的定向调用。该结果表明，在发送端目标任务不重训设置下，接收端仍可利用目标任务反馈驱动多轮增量重传。这一点将发送端训练从每个目标任务中解耦出来，是本文相对重训练式语义收发机的一个重要区别。

\section{本章小结}

本章面向业务流获得接入连接和无线资源承载后的可靠传输环节，针对星上轻量化语义收发机单次传输能力不足、完整语义表征重复传输开销较高的问题，提出协同增量语义 HARQ（CIS-HARQ）机制。该机制面向任务未知发送端与任务感知接收端构成的非对称收发架构，由接收端依据当前任务输出定位任务相关语义缺口并压缩为低开销硬掩码反馈，发送端利用该反馈生成面向请求分块的选择性语义增量，接收端通过跨轮语义累积保留有效历史语义并吸收当前增量。与现有方法依赖大参数量编码器、生成式复原模型或完整语义表征重复传输不同，本章方法将 HARQ 重传转化为接收端任务反馈引导的选择性语义增量重传过程，在控制星上模型复杂度和附加传输开销的同时，提升受限链路下任务相关语义信息的恢复可靠性。

在 ImageNet-100、CIFAR-100 与 VeRi-776 数据集、Rician 衰落信道、信噪比 $5$ dB 且信道带宽比（CBR）约 $0.02$ 的窄带宽条件下，CIS-HARQ 经三轮增量传输将下游任务精度由同初始信道带宽比下静态开环联合信源信道编码传输方法（SwinJSCC）的 $35.7\%$ 提升至 $65.0\%$，提升 $29.3$ 个百分点。该机制参数量约 $2.2$ M，星上推理时延毫秒级，较生成式扩散接收端（DiffJSCC）参数量减少约 $2500$ 倍，任务精度差距控制在约 $7\%$ 以内。在反馈有效性评估中，任务驱动反馈相较随机分块选择在 CIFAR-100 上将最终精度提升 $4.1$ 个百分点，在 VeRi-776 颜色与车型两个任务上平均提升 $1.4$ 个百分点；信道鲁棒性分析表明，在低信噪比（$2$~dB）与窄带宽（$\epsilon=32$）条件下，三轮增量重传带来约 $18$ 个百分点的精度增益。
